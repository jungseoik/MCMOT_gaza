# 레포 루트 .env 로 복사해 채운다 (.env 는 gitignore — 절대 커밋 금지)
# HuggingFace 토큰 — 비공개 backseollgi/MCMOT(ONNX·CAD·현장영상·리허설 영상) 읽기용.
# tools/fetch_assets.sh 가 자동으로 읽는다. `hf auth login` 으로 저장해도 동일하게 동작.
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


제일제당센터 영상/
# 멀티카메라 시스템 런타임 사이트 데이터(데모/운영) — 의도적 커밋만 허용
# 런타임 설정 백업(운영 스냅샷) — 저장소 밖 로컬 보관
*$py.class
# 238MB 샘플 mp4 모음
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
# 훈련영상 동기 송출 (ADR 08) — 송출 준비된 인코딩본은 대용량이라 HF 보관
# AI hub 도면 원본 일습(226MB) — 대용량 CAD는 커밋하지 않는다(HF 보관 대상)
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
!assets/sample1.mp4
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
build/
# Byte-compiled / optimized / DLL files
.cache
cache/
cad/17F.dwg
cad/17F.dxf
cad/17F_Egress Review(Sample).dwg
cad/17F_Egress Route.pdf
cad/17F_v2.dwg
cad/A-101_128_각 층 평면도_최종_수정.dwg
cad/A-101_128_각 층 평면도_최종_수정.dxf
# AI-Hub CAD: git엔 텍스트(문서·스크립트)만. 이미지·DXF·원본 dwg 등 대용량은 전부 HF.
cad/ai_hub_cad/*
!cad/ai_hub_cad/README.md
!cad/ai_hub_cad/render_plan.py
# target_floors/ 하위도 텍스트가 아니면 제외(png·dxf → HF)
cad/CAD_API_Based_Optimal_Evacuation_Route_Algorithm/*.dxf
# 원본 CAD(대용량 바이너리) — 로컬 보관/필요시 Git LFS. synth 실행엔 17F.dxf 필요
cad/ODATrial.tar.gz
cad/synth/out/
# 실제 현장 CCTV 원본 (개인정보·대용량) — HF backseollgi/MCMOT/field/ 에 보관, git 제외
celerybeat.pid
celerybeat-schedule
# Celery stuff
# C extensions
# Claude Code personal/local settings
.claude/settings.local.json
# 서드파티 모델 clone (비교 실험용, 대용량·외부 코드) — git 제외
#   commonly ignored for libraries.
*.cover
cover/
.coverage
.coverage.*
coverage.xml
cython_debug/
# Cython debug symbols
data/backups/
data/infer_profile.json
data/output/
# Data & output
data/sites/
data/vsource_leads/
data/vsource_logs/
data/vsource_state.json
data/vsource_stills/
db.sqlite3
db.sqlite3-journal
develop-eggs/
dist/
# Distribution / packaging
# Django stuff:
.dmypy.json
dmypy.json
docs/_build/
docs/manual/**/*.docx
docs/manual/**/img/
docs/manual/**/*.pdf
docs/reports/yolo_compare/runs/
docs/reports/yolo_compare/videos/
docs/reports/yolo_compare/weights/
downloads/
# 피난경로 알고리즘: 변환 DXF(대용량, dwg에서 재생성 가능)만 제외. dwg/zip/png/py는 추적
*.egg
*.egg-info/
.eggs/
eggs/
*.engine
.env
env/
ENV/
env.bak/
# Environments
external/reference/
external/weights/
field/
# Flask stuff:
#   For a library or package, you might want to ignore these files since the code is
# 항만/대피 합성 영상 작업물 (인라인 주석 금지 — gitignore는 줄 끝 주석 미지원)
# 재생성 가능한 합성 산출물(영상/GT/프리뷰)
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
# 사용 가이드 산출물 — HF backseollgi/MCMOT/manual/ 에 보관, git 제외
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
htmlcov/
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
.hypothesis/
#.idea/
# 추론 프로파일 선택(로컬 런타임 상태) — 배포 기본값은 INFER_PROFILE 환경변수로
#   install all needed dependencies.
.installed.cfg
# Installer logs
instance/
#   intended to run in multiple environments; otherwise, check them in:
#   in version control.
.ipynb_checkpoints
# IPython
ipython_config.py
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
# Jupyter Notebook
lib/
lib64/
local_settings.py
*.log
*.manifest
MANIFEST
# (원고.md·README.md 는 git 소관 — 편집 원본이므로 변경 추적이 필요)
# media/vsource — 영상은 전역 *.mp4 규칙으로 제외(HF 보관). 리허설 패키지 정의
# (rehearsal.json·매핑·도면·README)는 손작업 산출물이라 git 추적 (ADR 09)
media/vsource/**/*.avi
media/vsource/**/.cache/
# mkdocs documentation
*.mo
# Model weights & TRT engines
*.mp4
# mypy
.mypy_cache/
nosetests.xml
.nox/
# ODA 체험판 SDK(3자 바이너리) — 절대 커밋 금지
*.onnx
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
parts/
# pdm
.pdm-build/
#pdm.lock
.pdm-python
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
.pdm.toml
# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
# 외부 제공 참조 구현 (PIASPACE tracking_sample — 신규 프로파일 이식 원본).
pip-delete-this-directory.txt
# pipenv
#Pipfile.lock
pip-log.txt
# poetry
#poetry.lock
*.pot
profile_default/
.pybuilder/
# PyBuilder
__pycache__/
# PyCharm
*.py[cod]
*.py,cover
# pyenv
# PyInstaller
__pypackages__/
# PyPI configuration file
.pypirc
.pyre/
# Pyre type checker
.pytest_cache/
.Python
# .python-version
.pytype/
# pytype static type analyzer
results/clab_concat/
.ropeproject
# Rope project settings
.ruff_cache/
# Ruff stuff:
# SageMath parsed files
*.sage.py
.scrapy
# Scrapy stuff:
sdist/
share/python-wheels/
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
/site
*.so
*.spec
# Sphinx documentation
.spyderproject
# Spyder project settings
.spyproject
# src/clipreid_trt.py, system/ingest_ds/trt_infer.py 에 있다.
# 코드 자체는 우리 소유가 아니라 커밋하지 않는다. 이식 결과는 src/yolo26_trt.py,
target/
third_party/
#   This is especially recommended for binary packages to ensure reproducibility, and is more
.tox/
tracking_sample.zip
# Translations
# Unit test / coverage reports
#  Usually these files are written by a python script from a template
# UV
#uv.lock
var/
.venv
venv/
venv.bak/
# Videos (except samples)
.webassets-cache
wheels/
# YOLO26 비교 실험 산출물 (대용량 — 스크립트/JSON만 추적)

# 코어덤프(root 소유) · 리허설 시각화 산출물(영상, 재생성 가능)
/core
results/rehearsal_viz/
!.env.example
results/rehearsal_sessions/

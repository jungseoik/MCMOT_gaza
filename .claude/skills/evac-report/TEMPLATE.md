# 피난훈련 정량평가 — {세션ID}

> 대상 세션 `{session_id}` · {층 이름} · {훈련 일시} · 평가 구간 {지속}초
> 4대 정량지표(IDR·EPFI·CBS·SEI) 기준 해석.
> 종합점수·등급은 산출하지 않음(요구사항 D-8 — 고객 승인 가중치 확정 후 별도).

---

## 0. 요약

- {가장 중요한 발견 1문장 — 대개 미개시 구역 또는 미사용 비상구}
- {두 번째 발견}
- {세 번째 발견}
- 데이터 신뢰도: {ok/주의/불가} — {근거}

| 지표 | 값 | 방향 | 한 줄 판단 |
|------|-----|------|-----------|
| IDR | {개시 N/전체 M 구역}, 개시 구역 {a~b} m/s | 높을수록 좋음 | {} |
| EPFI | {평균} (중앙값 {}) | 높을수록 좋음 | {} |
| CBS | {합계} (최대 병목 {id} {값}) | **낮을수록 좋음** | {} |
| SEI | {값} / 100 | 높을수록 좋음 | {} |

---

## 1. 세션 개요

| 항목 | 내용 |
|------|------|
| 세션 ID | `{session_id}` |
| 층 / 사이트 | {floor_id} / {site_id} |
| 경보 시각 | {YYYY-MM-DD HH:MM:SS} |
| 경보 발생원 | {N}개 — {좌표 목록} |
| 평가 구간 | {지속}초 ({시작}~{종료}) |
| 관측 트랙 | {track_count}명 |
| 관측 카메라 | {cameras_observed} |
| 설정 버전 | calibration {v} / config {v} |
| 원본 | `{source 경로}` (녹화 `{record_db}`) |

![세션 타임라인](img/{session_id}_timeline.png)

{타임라인에서 읽히는 시점 서사 2~4문장. 언제 무엇이 변했는지.}

---

## 2. 데이터 신뢰도

| 항목 | 값 | 판단 |
|------|-----|------|
| 맵 투영 실패 비율 | {unmapped_point_ratio} | {} |
| 관측 트랙 수 | {track_count} | {} |
| 관측 카메라 | {목록} | {미관측 카메라 있으면 명시} |
| 종합 | **{ok / 주의 / 해석 불가}** | {} |

{verdict가 caution/insufficient면 이하 해석에 어떤 한계가 있는지 여기서 못 박는다.
ok면 2~3문장으로 충분.}

---

## 3. IDR — 구역별 피난 반응 확산

![IDR](img/{session_id}_idr.png)

| 구역 | 상태 | 개시 지연 | 경보원 거리 | IDR | 참여율 |
|------|------|-----------|-------------|-----|--------|
| {zone_id} | {개시/미개시} | {}초 | {}m | {} m/s | {}% |

**미개시 구역**: {있으면 가장 먼저·가장 길게 서술. 없으면 "전 구역 개시" 한 줄.}

{다중 경보원이면 idr_per_origin 편차 해석. 개시 구역 간 IDR 차이의 의미.}

> 현 임계값 기준 — v_th {}, a_th {}, r_th {}, Δt_hold {}초. (고객 확정 전 임의값)

---

## 4. EPFI — 권장 경로 충실도

![EPFI](img/{session_id}_epfi.png)

| 항목 | 값 |
|------|-----|
| 전체 평균 | {epfi_avg} |
| 중앙값 / 사분위 | {median} ({p25}~{p75}) |
| 최소 / 최대 | {min} / {max} |
| 경로 미배정 | {n}명 |
| 평균 이탈 거리 | {} m (허용 d_allow {} m) |

**이탈 상위**

| 트랙 | EPFI | 배정 경로 | 평균 이탈 | 최대 이탈 | 관측 |
|------|------|-----------|-----------|-----------|------|
| {} | {} | {} | {}m | {}m | {}초 |

{분포 해석. 평균이 높아도 하위군이 있으면 그것을 서술. 이탈 원인 가설.}

---

## 5. CBS — 병목 구역 혼잡 누적

![CBS](img/{session_id}_cbs.png)

| 병목 | CBS | 최대 밀도 | 임계 초과 | 위험도 |
|------|-----|-----------|-----------|--------|
| {} | {} | {} 명/㎡ | {}초 | {low/mid/high} |

{risk high 병목 우선 서술. peak_at_sec 기준 시점 서사.
CBS=0 병목은 통과량과 함께 해석(무사고 vs 미통과 구분).}

> 현 임계값 기준 — ρcrit {}, 가중치 w {}. (고객 확정 전 임의값)

---

## 6. SEI — 비상구 공간 활용 효율

![SEI](img/{session_id}_sei.png)

| 비상구 | 실제 통과 | 실제 분담 | 설계 분담 | Δ |
|--------|-----------|-----------|-----------|---|
| {} | {}명 | {}% | {}% | {+/-}%p |

**미사용 비상구**: {있으면 최우선 서술 — 설계상 분담이 있는데 통과 0명인 출구}

{쏠림 구조 해석. 총 통과 {N}명 대비 분포.}

---

## 7. 지표 간 관찰

{4지표를 가로질러 보이는 것. 예: "b1 병목 혼잡 급등 시점과 e1 쏠림 구간이 겹침 →
동일 원인(경로 편중)의 두 측면으로 보임".

⚠️ 여기서 합산·등급을 만들지 않는다. 관찰과 연결만 한다.}

---

## 8. 개선 권고

각 항목에 **근거 지표**를 명시한다.

1. **{제목}** — 근거: {지표·수치}. {권고 내용}
2. **{제목}** — 근거: {지표·수치}. {권고 내용}

{운영 개선 / 설비 점검 / 계측 개선을 구분하면 읽기 좋다.}

---

## 9. 부록 — 적용 파라미터 · 역추적

요구사항 §8(역추적성) — 이 레포트의 모든 수치는 아래 입력에서 재현 가능.

| 임계값 | 값 | 확정 여부 |
|--------|-----|-----------|
| v_th | {} | 고객 미확정(§9) |
| a_th | {} | 고객 미확정 |
| r_th | {} | 고객 미확정 |
| Δt_hold | {} 초 | 고객 미확정 |
| d_allow | {} m | 고객 미확정 |
| ρcrit | {} | 고객 미확정 |
| min_conf | {} | — |

- 세션 결과: `{source}`
- 원본 트랙 녹화: `{record_db}` — 임계값 변경 재계산 가능
  (`POST /api/session/{session_id}/replay`)
- 설정 버전: calibration {}, config {}
- 다이제스트 생성: `python tools/session_digest.py {session_id}`

> 임계값이 고객 확정으로 바뀌면 위 녹화본으로 **같은 세션을 재계산**해 이 레포트를
> 갱신할 수 있음.

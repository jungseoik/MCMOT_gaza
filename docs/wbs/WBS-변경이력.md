# C-LAB PoC WBS 변경이력 (CHANGELOG)

WBS(`C-lab_PoC_WBS_vN.xlsx`)는 **바이너리(xlsx)라 git diff가 안 되므로**, 버전 간
"무엇이 / 어디가 / 왜" 바뀌었는지를 이 파일에 **사람이 읽을 수 있게** 누적 기록한다.

- **버전 컨벤션**: 매주 점검 후 수정이 있으면 `vN → vN+1`로 올린다.
- **최신본 위치**: 그 주차 폴더 `docs/weekly/YYYY-MM-N주차/C-lab_PoC_WBS_vN.xlsx`.
  (예전 버전 아카이브는 `docs/wbs/`에 보관 — 현재 `v3`·`v4`)
- **점검 절차**: `/wbs-review` 스킬 → 진척 보고서 `docs/weekly/<주차>/WBS-진척점검.md` 생성.
- 컬럼: `상태`(H열) = 예정 / 진행중 / 완료. `완료 일정`(G열).

> **현재 최신**: **v8** @ `docs/weekly/2026-07-4주차/` (07-4주차, DeepStream 다채널 전환 — **90개**, 완료 20/진행중 11/예정 59)

---

## v8 — 2026-07-20 (7월 4주차)

- 07-4주차 시작. v7을 `cp -p`로 복사해 `docs/weekly/2026-07-4주차/C-lab_PoC_WBS_v8.xlsx` 생성.
- 이번 주 성과 = **DeepStream zero-copy 다채널 인제스트 전환**(P0~P11, 커밋 `236aa2b`~`30a0a09`).
  기존 ffmpeg 경로(4ch@5fps 한계)를 zero-copy 디코드+배치 추론으로 교체 → **1GPU 16ch@5fps
  (총 78.7fps, 약 4배)** 실측, 출력 유사도(트랙 매칭 98.95%)·운영 webui E2E·b32 한계 재스윕까지.
- **상태 변경 6건 (항목 추가/삭제 없음, 총 90 불변)**:
  - `4.2.3` 혼잡도 지표 산출 **진행중 → 완료**: 구역별 순간 인원수·순간 밀도
    (`system/metrics/engine.py:407-416`) + 시간적분(평균)·**Peak** 추적(`system/metrics/session.py:305-306,473`).
    v5에서 지적된 Peak Occupancy 미구현 갭 해소.
  - `4.7.1` 1채널 통합 파이프라인 검증 **진행중 → 완료**: 운영 webui(:8900) DS 모드에서 5대 분석
    (추적·속도·구역 밀도·출구 카운트·정렬/체류) 실시간 통합 E2E 검증
    (`docs/reports/DeepStream-webui-통합검증.md`).
  - `4.7.2` 15채널 동시 처리 확장 **예정 → 완료**: DS 배치 추론(직렬 락 해제)+카메라별 BoostTrack
    분리로 1GPU 16ch@5fps 실측(`docs/reports/DeepStream-한계처리량-실측.md §2`,
    `system/ingest_ds/launcher.py` `DsIngestManager`) — 15ch 목표 초과.
  - `4.7.3` 150채널 스케일 달성 **예정 → 진행중**: `WORKERS_PER_GPU` 워커 분할 + 부하 균등
    (`launcher.py:77-105` `partition_cams`) 확보. 150ch 실달성·추적 경량화 미완.
  - `4.7.4` 스케일 성능 검증 **예정 → 진행중**: 채널당 fps·총 throughput·VRAM·지연 스윕
    12~64ch 실측(`한계처리량-실측 §2·§7·§9`). 24h 연속 안정성(6.2.4) 미측정.
  - `6.3.2` 성능 튜닝(TensorRT·Batch Size) **예정 → 진행중**: DS 배치 추론·dynamic TRT 엔진
    (b16/b32)·배치 상한 인자화 구현·실측(`한계처리량-실측 §9`). b32 이득 없음으로 기각·b16 유지.
- 진척(총 90): **완료 20 / 진행중 11 / 예정 59** (v7 대비 +3 완료 / +1 진행중 / −4 예정).
- 참고: `min_conf`/`valid_roi` 카메라 설정 UI(`925bb38`·`9217348`·`70ca59d`·`d624faf`)는 5.1.4
  전 범위(사용자 관리·시스템 로그) 미달로 **상태 변경 없이 유지**(과대평가 금지). TRT 비블로킹
  스트림 레이스(`9b2ba76`)·워커 죽음의 나선(`02bdc58`) 버그 수정·재검증 완료.
- 근거: [WBS 진척 점검](../weekly/2026-07-4주차/WBS-진척점검.md) ·
  [DeepStream zero-copy 인제스트 전환 ADR](../architecture/04-DeepStream-zero-copy-인제스트-전환.md).
- ⚠️ v8 xlsx는 openpyxl로 상태(H)·완료일정(G)·색·버전셀(C5)만 in-place 편집. 셀 diff로 6개
  항목의 G·H·C5만 변경 확인(추가/삭제 0). 간트 색상 등 서식은 Excel 육안 검토 권장.

## v7 — 2026-07-01 (7월 1주차)

- 07-1주차 시작. v6를 `cp -p`로 복사해 `docs/weekly/2026-07-1주차/C-lab_PoC_WBS_v7.xlsx` 생성.
- **단일 채널 검증 반영**: `6.2.1` 추적·지표 신뢰성 검증 **예정 → 진행중**(AI-Hub TRACE 정량
  평가 근거 — IDF1 0.834, 검출 PRF, Count MAE).
- **지도 정합·도면 자산 반영**: `4.4.1` 지도 좌표 매핑 **예정 → 완료**(2D map registration
  모드 구현 `ef044c9` + 17F 평면도 확보), `1.1.4` 피난 동선 정의 **예정 → 진행중**(17F
  평면도·**피난경로 PDF** 확보). (4.4.2 좌표 변환은 기존 완료)
- 진척(총 90): **완료 17 / 진행중 10 / 예정 63**.
- 근거: [개발 현황·단일채널 테스트 결과 보고](../weekly/2026-07-1주차/2026-07-01_개발현황-및-단일채널-테스트-결과-보고.md).
  Depth 거리 측정/협착 근접감지는 현 WBS(피난 중심) 미반영 — 스코프 편입 여부 협의 대상.

## v6 — 2026-06-22 (6월 4주차)

06-4주차 시작(v5 → `docs/weekly/2026-06-4주차/C-lab_PoC_WBS_v6.xlsx`)에 더해, CJ제일제당
피난대피훈련 PoC 계획과 정합되도록 **실증 관련 항목 보강 + 2건 추가(92 → 94)**.

**텍스트 수정(내용 D셀) — 3건**:
- `2.1.4` 인원 규모 `10/50/100인` → `규모별(예: 50/100/200, 실제 규모 확정 예정)`, "시뮬레이션" → "실훈련 시나리오".
- `6.1.1` 규모 기준 통일 + **CJ제일제당** 맥락 명시.
- `6.1.2` "복합 이상상황 시뮬레이션" → "복합 이상상황(실훈련 기반)".

**신규 항목 추가 — 2건(대분류 6 / 중분류 `현장 실증 수행`)**:
- `6.1.5` 실증 사전 준비(현장 캘리브레이션·구역/비상선 정의·다채널 셋업·파라미터 협의, 1w).
- `6.1.6` 실증 중 실시간 대시보드 모니터링 및 종합평가 연계(3d).

**PoC 정합 정리(추가, 94 → 90)**:
- **6.2 "성능 검증" 재정의**: PoC는 *이상상황 탐지 성능*이 아닌 *피난지표 신뢰성·시스템 성능* 검증.
  `6.2.1`→"추적·지표 신뢰성 검증", `6.2.2`→"피난 이벤트 산출 정확도", **`6.2.3`(오탐/미탐 분석) 삭제**.
  `2.2.4` 정량 기준에서 `mAP≥0.85`(검출 GT 필요) 제거 → 추적 신뢰성(IDF1)·지연 중심.
- **3.1.x 환경 완료 반영**(실제 2GPU 서버 기준): `3.1.1`(Ubuntu 24.04·Docker·NVIDIA Toolkit·
  GPU 2장)·`3.1.2`(PyTorch·TensorRT, 서빙 FastAPI — Triton/ONNX Runtime 미사용)·`3.1.3`(모듈)
  → **완료**. 내용도 실측 정정(22.04→24.04, K8s/Triton 등 미사용 반영). `3.1.5`(모니터링)은 예정 유지.
  `1.2.2` GPU 환경 검토 내용도 실제 GPU로 정정: `A100/RTX 4090·CUDA 12·TRT 8` →
  `RTX PRO 6000 Blackwell(2장, ~96GB)·CUDA 13·TRT 10`.
- **대분류 7 보고서 통합**: `7.2.1`을 **최종 결과 보고서(통합 1종)**로 재정의, **`7.2.2`(KPI 기준서)·
  `7.2.3`(데이터 구조 문서)·`7.2.4`(운영 가이드) 삭제**. `7.1.x`(분석) 유지.

> 인원 규모(`6.1.1`)·간트 일정은 실증 일자/규모 확정 후 재조정(현재 잠정), 간트(베이스라인) 이동 보류.
> 진척: **완료 16 / 진행중 8 / 예정 66 (총 90)**.

## v5 — 2026-06-17 (6월 3주차)

주차별 관리 컨벤션 시작(v4 사본 → `docs/weekly/2026-06-3주차/`)에 더해, 06-3주차 점검으로
**6건을 진행중 → 완료** 처리. xlsx는 openpyxl로 상태(H)·완료일정(G) 셀만 in-place 편집.

**완료(진행중→완료, 완료일 2026-06-17) — 6건:**
| WBS | 항목 | 근거 |
|-----|------|------|
| 3.1.4 | 성능 테스트(처리량·VRAM·지연) | `docs/reports/` 벤치, `optimization-report.md` |
| 3.2.1 | RTSP 스트림 연동 | `webui/server.py` `/rtsp`, `src/inference_gpu` `stream(live)` |
| 4.1.3 | 이동 궤적 정규화 | `webui/speed.py` history + ROI/`_world` 보정 |
| 4.3.2 | 출입 방향 판별 | `webui/counter.py` 부호거리 + inside_sign |
| 4.3.3 | 잔류인원 산출 | `webui/counter.py` `occ=start+in−out` |
| 4.4.2 | 좌표 변환(Homography) | `webui/depth_ground.py` + 수동 4점 |

**모델명 정정(내용 D셀)**: WBS 표기를 실제 구현 모델로 수정 — `3.1.3`·`4.1.1` 탐지
`YOLOv8x/RT-DETR-L → YOLOX-MOT20`, `3.1.3`·`4.1.2` 추적 `BoT-SORT/ByteTrack → BoostTrack++`,
`3.1.3` 재식별 `CLIP-ReID → FastReID(SBS-S50)`.

**신규 항목 추가(85 → 92) — 북극성 누락 요구 3건 + 채널 스케일업 4건**: 번호순 위치에
행 삽입(병합셀 범위 재구성·블록 확장·신규 병합).
- 누락 요구 3건: `4.1.6` 방향성 정렬도(✅완료, `webui/speed.py` 이미 구현), `4.3.7` 비상구 고유
  통과 dedup(예정), `4.3.8` 구역별 피난 개시 시점(예정).
- 채널 스케일업 4건(대분류 4 신규 중분류 `4.7 다채널 스케일업`): `4.7.1` 1채널 통합 검증(진행중,
  2d), `4.7.2` 15채널 동시 처리(예정, 2w), `4.7.3` 150채널 스케일 달성(예정, 3w), `4.7.4`
  스케일 성능 검증(예정, 1w). [북극성 채널 스케일 요구사항](../requirements/CCTV-영상분석-엔진-필수추출정보.md) 반영.

**셀 변경(v4→v5 diff, 총 16셀)**: 위 6행 각 `상태(H)=완료`·`완료일정(G)=2026-06-17`(+ 완료셀
색을 초록 `C6EFCE`로 통일), 내용(D) 3셀(모델명), 버전 셀(C5). (그 외 기존 셀 불변)

**진척 요약(92개)**: **완료 13 / 진행중 8 / 예정 71**. 상세·정합성은
[WBS-진척점검](../weekly/2026-06-3주차/WBS-진척점검.md) 참조.

## v4 — 2026-06-16 (구현현황 1차 반영)

v1 이후 실제 코드/엔진 구현 상태를 WBS 상태컬럼에 처음 반영. **상태 변경 25건**.

**완료(예정→완료, 완료일 2026-06-16) — 6건, 전부 `4. AI 분석 기능 개발`:**
| WBS | 항목 | 레포 근거 |
|-----|------|-----------|
| 4.1.1 | 객체 탐지 (Person Detection, TensorRT) | `src/inference_trt.py`(TRTDetector), YOLOX-MOT20 엔진 |
| 4.1.2 | 단일 카메라 추적 (MOT, Kalman) | `tracker/`, `src/inference_gpu.py`(BoostTrack++) |
| 4.1.4 | 이동 특성 산출 (이동거리·속도·방향·체류) | `webui/speed.py`(SpeedEstimator) |
| 4.1.5 | 통행량 분석 (Line Crossing In/Out) | `webui/counter.py` |
| 4.2.1 | 속도 지표 (Pixel-to-Meter / Homography) | `webui/speed.py`, `webui/depth_ground.py` |
| 4.2.2 | 체류 지표 (Dwell Time) | `webui/speed.py`(dwell) |

**진행중(예정→진행중) — 19건:**
- `3.1.4` 성능 테스트, `3.2.1` RTSP 스트림 연동
- `4.1.3` 이동 궤적 정규화, `4.2.3` 혼잡도 지표, `4.2.5` 대시보드 연동,
  `4.3.2` 출입 방향 판별, `4.3.3` 잔류인원 산출,
  `4.4.2` 좌표 변환, `4.4.3` 구역 상태 시각화, `4.4.4` 이동 흐름 표현,
  `4.5.4` 대피 경로 시각화
- `5.1.1` 인원 현황 시각화, `5.1.2` 혼잡도 표시

**버전 셀**: `v1` → `v4 (2026-06-16 갱신: 구현현황 반영)`

## v1~v3 — 2026-06-04 (초안)

- v1: 최초 WBS 작성(85개 작업, 7개 대분류, 4개월 일정). 전 항목 `예정`.
- v2·v3: 구조/표현 보정(상태 변경 없음).
- 아카이브로 `docs/wbs/`에 `v3` 보관(v1·v2 파일은 별도 미보관).
